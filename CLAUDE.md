# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

프로젝트 원칙은 [AGENTS.md](AGENTS.md)에, 전략 규칙과 결정 이력은 [docs/STRATEGY.md](docs/STRATEGY.md)에
있습니다. 이 파일은 그 둘에 없는 작업용 정보만 다룹니다.

## 현재 상태

- 백테스트, 신호 생성, **토스증권 Open API 자동매매**까지 구현돼 있습니다.
  주문 코드는 `toss_api.py`(HTTP) → `executor.py`(안전장치) → `alerter.py --live`(실행) 순서입니다.
- **실제 주문은 `--live` 를 명시할 때만 나갑니다.** 기본은 읽기 전용, `--execute` 는 드라이런.
  이 기본값을 바꾸지 마세요. `config/HALT` 파일이 있으면 모든 주문이 차단됩니다.
- **실API 검증은 일부만 됐습니다** (2026-09-22, accountSeq=1). 조회 전부 + 수량 기반
  LIMIT 주문·체결 확인은 통과(프리장 SOXS 1주 매도 FILLED). **하지만 전략 매수가 쓰는
  `orderAmount`(금액 주문)와 SWITCH의 매도→매수 연속 자금은 미검증입니다.**
  전체가 검증됐다고 말하지 마세요. 항목별 현황은 `toss_api.py` 독스트링에 있습니다.
- **IP 허용 목록**이 선행 조건입니다. 미등록이면 토큰 발급이 403 `access_denied
  "IP address not allowed"`. 가정용 IP는 바뀌므로 무인 운전의 조용한 실패 원인입니다.
- 개발 3단계(슬리피지 검증)를 **건너뛴 상태**입니다. AGENTS.md 기본 원칙 참고.
- **2026-09-23부터 무인 운전 중입니다.** 사용자가 "한 달간 개입하지 않고 봇에 맡긴다"고
  했고, 킬 스위치를 풀었습니다. 저널은 증권사 잔고와 대조해 맞춰 뒀습니다(SOXS 151.31주).
  - **현재 포지션은 사용자가 수동으로 넣은 예외입니다.** 규칙상 그 자리는 `NONE`(거부권이
    상승 추세 SOXS 진입을 차단)이었는데 사용자가 직접 매수했고, "예외로 처리하라"고
    지시했습니다. 청산은 횡보 평균회귀 규칙에 맡깁니다. 성과 통계에서는 빼세요.
  - **크론 구성** — 매일 08:30 `watchdog.sh --check`(문제 있을 때만 알림), 매주 월 09:00
    `--report`(주간 요약), **평일 22:25 `brief.sh`**(개장 5분 전 요약 알림),
    22:00~01:59 15분마다 `daily_trade.sh --live`(실제 매매).
    조용히 멈추는 것이 이 봇의 최대 위험이라 감시를 따로 둡니다.
  - **개장전 요약은 짧게 유지하세요** (`notifier.format_brief`, 사용자 요청 2026-09-24).
    행동 / 포지션(현금이면 금액, 주식이면 평가금액) / 레짐 **셋만** 넣습니다. 매일 밤
    오는 알림이라 길어지면 안 읽게 됩니다. 자세한 건 레짐 콘솔에서 봅니다.
    `--brief` 는 **읽기 전용**이고 `--execute/--live` 분기 뒤에 있어 주문 경로로 갈 수 없습니다.
  - 사이징 자산은 **봇이 굴리는 포지션 평가금액**입니다(아래 '사이징 자산' 항목).
    손으로 갱신할 필요가 없어졌습니다.
- `journal/trades.csv`에 실거래 기록이 쌓이는 중입니다. 백업은 `trades.csv.bak-*` 입니다.
- git 저장소가 **아닙니다**. 파일을 지우면 복구할 수 없습니다.
- 린트 설정은 없습니다. 테스트는 `tests/test_executor.py`(72건)와 `tests/test_consistency.py` 둘입니다.
- `.venv`는 `../soxl-soxs-bot/.venv` 심볼릭 링크입니다. Python 3.9라
  `X | None` 문법은 `from __future__ import annotations` 덕분에 어노테이션에서만 됩니다.

## 명령어

```bash
./.venv/bin/python src/backtest.py                    # 최근 3년 (기본값)
./.venv/bin/python src/backtest.py --since 2023-01-01 # AI 국면
./.venv/bin/python src/backtest.py --years 0          # 전체 기간
./.venv/bin/python src/alerter.py                     # 오늘의 지시만 (읽기 전용)
./.venv/bin/python src/alerter.py --execute           # 안전장치 검사 + 드라이런
./.venv/bin/python src/alerter.py --live --notify     # 실제 매매 + 보고 (되돌릴 수 없음)
./.venv/bin/python src/alerter.py --toss-accounts     # accountSeq 확인
./.venv/bin/python src/alerter.py --slippage          # 신호가 대비 체결가
./.venv/bin/python src/alerter.py --brief             # 개장전 요약 알림 (읽기 전용)
./.venv/bin/python scripts/build_dashboard.py         # 대시보드 데이터+HTML 생성
./scripts/console_launch.sh                           # 레짐 콘솔 (크롬 앱 창) = 레짐콘솔.app
./.venv/bin/python src/watchdog.py --report --no-notify  # 주간 요약을 화면으로
./scripts/serve.sh                                    # 레짐 콘솔 웹 버전 — 폰에서 봐야 할 때만
./.venv/bin/python tests/test_executor.py             # 안전장치·멱등성·부분봉·체결시각·자본하한 72건
./scripts/daily_trade.sh --execute                    # 무인 실행 스크립트 드라이런
./.venv/bin/python tests/test_consistency.py          # 백테스트=알리미 판단 일치
./.venv/bin/python -m compileall src                  # 문법 점검
```

주문 관련 코드를 건드렸으면 `tests/test_executor.py` 를 반드시 돌리세요.

## 수치 기준 (문서와 코드가 어긋나면 코드를 믿으세요)

원금 $5,000, 노출 100%, 왕복비용 0.2% 기준. **벤치마크와 함께 보고할 것.**

백테스트와 실거래가 같은 판단 경로를 쓰므로 수치는 **하나**입니다.

| 구간 | 전략 | MDD | Sharpe | 거래 | 승률 | 세후 | SOXL 보유(세후) |
|---|---|---|---|---|---|---|---|
| **최근 5년 (평가 기준)** | **41.75배** | -60.9% | 1.42 | 60건 | 58.3% | 23.68배 | 3.22배 (2.81) |
| 최근 3년 (CLI 기본) | 45.96배 | -52.0% | 1.85 | 31건 | 67.7% | 29.67배 | 8.45배 (6.89) |
| AI 국면 2023~ | 80.68배 | -52.0% | 1.78 | 42건 | 66.7% | 46.67배 | 14.71배 (11.77) |
| 전체 16.3년 | 159.26배 | -90.9% | 0.80 | 188건 | 58.0% | 59.79배 | **238.86배** (186.61) |

- 위 표는 **2026-09-24 측정**입니다. 최근 N년 창은 매일 한 칸씩 밀리므로 배수가 조금씩
  움직입니다(예: 09-23 측정에서는 최근 5년이 42.59배였습니다). 정확한 값이 필요하면
  `./.venv/bin/python src/backtest.py --years 5` 를 직접 돌리세요.

- **⚠️ 이 수치를 일반적인 엣지의 증거로 쓰지 마세요** (2026-09-24, STRATEGY.md 6.7).
  같은 규칙을 파라미터 하나 안 바꾸고 다른 레버리지 쌍 7개에 적용했더니 **전부
  실패**했습니다(승률 58%→35~51%, 대부분 매수보유에 패배). 이 규칙은 **반도체의
  평균회귀 성질에 맞춰진 것**이고, 그게 진짜 엣지인지 5년 데이터에 맞춘 결과인지는
  백테스트로 더 구분할 수 없습니다. 기간을 16.3년으로 늘리면 매수보유가 이깁니다
  (241.61배 vs 159.26배). 남은 검증 수단은 **손대지 않은 미래 구간(실거래)** 뿐입니다.
  - 또한 이 전략은 사실상 **롱 편향 기계**입니다 — 포지션 보유 시간의 **92%가 롱**이고
    숏은 최근 5년 기여가 1.59배뿐입니다. "양방향 전략"이라고 설명하지 마세요.
  - **다른 종목으로 확장하자는 제안이 오면 6.7 표를 먼저 보여주세요.** 종목마다
    파라미터를 다시 맞추는 것은 검증이 아니라 과적합의 반복입니다.
- **평가 구간은 최근 5년입니다** (사용자 지시, 2026-09-23). 2010~2022년 11년을 학습
  구간으로 쓰지 마세요. 규칙 채택을 판정할 때는 최근 5년을 **앞 2.5년(학습) / 뒤
  2.5년(검증)** 으로 잘라 양쪽을 모두 보고, 어느 경계를 썼는지 함께 적습니다.
- 전체 기간에서는 SOXL 매수보유가 훨씬 낫습니다(234배 vs 159배, 세후 183배 vs 60배).
  이 사실을 빼고 보고하지 마세요. **최근 5년에서는 전략이 앞섭니다**(42.59배 vs 3.36배).
- **AI 국면은 파라미터 탐색(6.4)과 분류기 선택(3.1)의 평가 구간으로 썼습니다.**
  out-of-sample이 아닙니다. 다음 검증에는 손대지 않은 다른 구간이 필요합니다.
- 낙폭이 큽니다. 최근 5년 -60.9%, 전체 -90.9%. 노출을 낮추면 함께 줄어듭니다.
- **손절은 없습니다** (2026-09-23, STRATEGY.md 4.5). 하루 동안 고정 10% → 0.85σ →
  제거로 갔습니다. 최근 5년 기준으로 **어떤 손절도 개선이 아니었습니다**(없음 29.65배 /
  10% 27.36배 / 0.85σ 21.62배). 청산은 레짐 이탈 · 중심선(z=0) · 반등 후 꺾임 셋뿐입니다.
  손절을 다시 제안하려면 최근 5년 분할로 양쪽 모두 개선되는지부터 보이세요.
  **그리고 오버나이트 갭 때문에 어떤 손절도 지정가에 걸리지 않는다는 점**(STRATEGY.md
  7.2)을 함께 보세요 — 갭을 없애는 방법은 전부 측정했고 통하지 않았습니다.
- **장기 추세 거부권이 있습니다** (2026-09-23, STRATEGY.md 4.6). **같은 날 채택 →
  제거 → 재채택**을 거쳤습니다(전부 사용자 지시, 백테스트 수치는 세 번 내내 그대로
  거부권이 있을 때가 더 좋았습니다 — 최근5년 29.65→42.59배). 지금은 **켜져 있습니다.**
  횡보·하락 레짐의 평균회귀 진입 중 120일 추세를 거스르는 쪽을 차단합니다. 값을
  옮기려면 6.6/4.6 표를 먼저 보세요.
- **분할매수는 적용하지 않았습니다** (2026-09-23, STRATEGY.md 4.7). 9개 조합 전부
  검증 구간이 기존보다 나빴습니다. 기각 목록 참고.
- 위 수치는 규칙을 바꾸면 전부 무효입니다. 바꿨으면 다시 돌려서 갱신하세요.

## 아키텍처에서 알아둘 점

- **장기 추세 거부권이 있습니다** (2026-09-23, STRATEGY.md 4.6). 횡보·하락 레짐의
  평균회귀 진입 중 **120일 추세를 거스르는 쪽을 차단**합니다(데드밴드 = 후행 확장 0.5σ).
  상승 레짐 추세 진입에는 적용하지 않습니다. 최근 5년 29.65→42.59배, 전체 103→159배.
  기간 60~150일이 고른 고원이고 250일은 무너지므로, **값을 옮기려면 6.6 표를 먼저 보세요.**
  같은 날 채택→제거→재채택을 거쳤으니(4.6), 다시 끄자는 얘기가 나오면 이 이력부터 보여주세요.
- **레짐은 세 조건을 모두 만족해야 상승/하락입니다** — 50일선 위치 + 50일선 기울기 +
  **가격 20일 변화**. 가격 조건은 MA 기울기의 후행성을 보정하려고 2026-09-22에 넣었습니다
  (STRATEGY.md 3.1). **MA 기간을 줄이는 방식은 실패**했습니다 — 전환이 2배로 늘어
  휩쏘 비용이 이득을 삼킵니다(15~30일에서 전체 0.27~18.94배). 다시 시도하지 마세요.
- **판단은 `strategy.decide_action()` 한 곳에만 있습니다.** 백테스트와 알리미가 이 함수만
  쓰고, `tests/test_consistency.py` 가 16.3년 전 구간에서 같은 행동이 나오는지 확인합니다.
  **여기에 판단 로직을 복제하지 마세요.** 예전에 백테스트와 알리미가 각자 로직을 갖고 있어
  같은 전략이 48배 vs 83배로 갈렸습니다.
- **목표 포지션 모델**입니다. 매일 목표를 정하고 현재와의 차이만 행동으로 만듭니다
  (HOLD / OPEN / CLOSE / SWITCH). 같은 방향 재진입은 거래가 아니라 `RESTAMP`(규칙 갱신)로
  처리해 왕복 비용을 내지 않습니다. `SWITCH`는 같은 장에서 매도→매수 두 주문입니다.
- **신호는 SOXX, 체결은 SOXL/SOXS.** 체결가는 항상 신호 **다음 거래일 시가**입니다.
  이 규칙을 깨면 결과가 부풀려집니다.
- **레짐 전환이 강제청산을 유발합니다.** 50일선 레짐은 평균 12일로 짧아, 익절·손절·트레일링이
  걸리기 전에 레짐이 먼저 바뀌는 경우가 많습니다. 트레일링 폭을 10~40%로 바꿔도 결과가
  평평한 이유가 이것입니다(거의 발동하지 않음).
- **청산 규칙은 진입 당시 레짐의 것을 끝까지 씁니다.**
- **마지막 날 보유 포지션은 종가로 강제 청산**해 거래 통계에 포함합니다(`open_at_end`).
  이 처리가 없으면 미청산 평가이익이 자산곡선에만 반영되고 거래 통계에서 빠져 지표가 어긋납니다.
- **자산곡선은 일별 평가**라 MDD가 정확합니다.
- **SOXS 가격은 Yahoo 소급조정 값**입니다. 절대가는 실제 호가가 아니고 수익률만 정확합니다.
  리버스 스플릿 보정 오류도 있습니다(2026-05-26 SOXS -94.58% vs 이론 -18.29%).
- `src/`를 `sys.path`에 넣어 import하므로 `signal.py`라는 이름은 표준 라이브러리를 가립니다.
  그래서 `strategy.py`입니다.

## 알리미 · 실행

- `alerter.py`가 일봉 종가로 그날의 지시(OPEN/CLOSE/HOLD/NONE)를 만들고, `journal.py`가
  현재 포지션을 복원합니다. **신호가와 실제 체결가를 따로 기록**해 슬리피지를 추적합니다.
- `--execute`/`--live` 면 `executor.build_plan()` → `preflight()` → `execute()` →
  `settle()` → `record()` 순서로 갑니다. 안전장치는 **`preflight()` 한 곳에만** 모읍니다.
- 매수는 **금액 주문**(`orderAmount`, US MARKET 전용), 매도는 **수량 주문**(판매가능 전량)입니다.
  둘 다 정규장 시작~종료 1시간 전에만 접수되므로 executor가 그 구간을 강제합니다.
- `clientOrderId`는 `soxx-{신호일}-{액션}-{종목}`으로 고정돼 있어 같은 날 재실행해도
  중복 주문이 나가지 않습니다(서버 멱등성 10분 + journal 기록으로 그 이후도 차단).
- 토스 토큰은 client당 1개만 유효합니다. **같은 자격증명으로 두 프로세스를 동시에 돌리면
  서로의 토큰을 무효화합니다.**
- 미체결 건이 있으면 실행 시 상단에 경고가 뜹니다. `--fill` 또는 `--skip`으로 처리하세요.
- **CSV 값은 전부 문자열입니다. `if r["필드"]` 로 걸러내면 `"0"`·`"0.0"`이 참이 됩니다.**
  이 함정을 두 번 겪었습니다: `fill_price`(0으로 나누기), `equity_after`(자산이 $0으로
  읽혀 주문 금액이 $0이 됨 — cron 시험에서 발견). 새 필드를 읽을 때도 **수치로 판정**하세요.
- **사이징은 `risk_manager.position_size_usd()` 한 곳에만 둡니다.** `executor.build_plans()`가
  `equity * exposure` 를 다시 계산했다가 자본 하한이 빠져, 화면엔 $5,000이 뜨고 실제 주문은
  $0으로 나간 적이 있습니다. executor는 **계산된 금액만 받습니다.**
- **현재가·기준가는 토스 실호가를 씁니다** (2026-09-23, `alerter.live_prices`).
  Yahoo의 SOXL/SOXS는 소급조정 시계열이라 실호가와 2% 가까이 어긋납니다(실측 SOXL
  +1.97%, SOXS -1.95%). 화면·텔레그램의 평가손익과 저널의 `signal_price`(슬리피지 기준)가
  그만큼 틀어지므로 실호가를 우선하고, 못 받으면 Yahoo로 물러납니다. 화면에
  **"(토스 실호가)" / "(Yahoo 종가)"** 로 출처를 밝힙니다.
  - **⚠️ 신호 계산은 Yahoo 일봉 그대로입니다. 여기에 토스를 섞지 마세요.** 레짐·z는
    일봉으로 계산하는데 **토스에는 과거 일봉 API가 없고**, 백테스트 수치가 전부 Yahoo
    시계열 위에 서 있습니다.
  - **고점(트레일링)과 `Holding` 에 넘기는 값도 Yahoo 그대로 둡니다.** 현재가만 다른
    소스로 바꾸면 고점과 자가 달라져 판정이 어긋나고, `tests/test_consistency.py` 가
    보장하는 "백테스트=알리미" 관계가 깨집니다. **표시·기준가에만 씁니다.**
- **사이징 자산 = 봇이 굴리는 포지션의 평가금액입니다** (2026-09-24 확정,
  `alerter.resolve_equity` → `toss_api.strategy_equity_usd`). 매 실행마다 증권사에서
  SOXL/SOXS 평가금액을 읽습니다. 조회가 실패하거나(IP 미등록·자격증명 없음) 포지션이
  없으면 journal의 마지막 값(= 직전 청산 대금)으로 물러나고, **어느 쪽을 썼는지 화면에
  출처를 찍습니다.** 숨기지 마세요.
  - **⚠️ USD 예수금을 자본에 더하지 마세요.** 한 번 그렇게 만들었다가 2026-09-24에
    사용자가 바로잡았습니다 — 수수료·예비 목적으로 $724를 입금하자 자산이
    $5,359→$6,359로 뛰어 다음 주문이 27% 커질 뻔했습니다. 이 계좌에는 국내 주식과
    BRK·XXRP 등 봇과 무관한 종목도 섞여 있습니다.
  - **계좌의 USD 현금은 자본이 아니라 예비금입니다.** 전략 자본이 자본 하한
    `min_capital_usd`($5,000) 아래로 떨어졌을 때 채워 넣는 재원입니다 —
    `risk_manager.topup_needed()` 가 현실에서 어디서 돈을 끌어오는지가 이 현금입니다.
    watchdog이 "자본 하한 미달인데 예비금 부족" 상황을 감시합니다.
  - **입금·출금은 전략 자본을 바꾸지 않습니다.** 성과를 "투입대비"로 보는 이유와 같습니다.
  - 포지션 평가액이므로 **이익은 그대로 복리로 굴러갑니다.** 이전에는 journal의
    `equity_after` 만 읽었는데 `executor.record()` 가 받은 값을 그대로 다시 적을 뿐이라
    자산이 영원히 고정이었습니다. **`executor` 는 건드리지 않았습니다.**
  - SWITCH의 매수 다리는 `fund_buy_plan()` 이 실제 매수가능액으로 다시 맞춥니다
    (해외주식 결제 T+1이라 매도 직후 예수금이 늦게 반영될 수 있음).
- **체결 시각은 개장 직후만 유효합니다.** 1시간봉 35건 실측에서 진입·청산 64개 조합 중
  09:30 개장이 최고였고 12:30부터 반토막(30.9배 → 16.2배)입니다. executor가 개장 후
  30분을 목표로, 120분 초과면 **그날을 건너뜁니다**. 늦게 체결하는 것보다 포기가 낫습니다.
  이 상한을 늘리자는 제안이 오면 STRATEGY.md 7.1을 먼저 보여주세요.
- **미완성 당일 봉을 쓰면 안 됩니다.** `data_fetcher.drop_incomplete_bar()` 가
  정규장 중 호출 시 당일 봉을 잘라냅니다. 주문 시간대가 곧 당일 봉 미완성 시간대라
  이 가드가 없으면 장중 가격을 종가로 착각합니다.
- **레짐 콘솔은 둘입니다. 상태 계산은 `server.build_state()` 한 곳에만 있습니다.**
  대시보드(`results/dashboard.html`, cron이 1시간마다 만드는 스냅샷)와는 다른 물건으로,
  둘 다 15초마다 스스로 갱신하는 **읽기 전용** 도구입니다. **둘 다 주문 기능이 없습니다**
  — 안전장치를 우회하는 두 번째 주문 경로를 만들지 않기 위해서입니다.
  - **레짐콘솔.app / `scripts/console_launch.sh` (평소에 쓰는 쪽)**: 로컬 서버를 띄우고
    **크롬 `--app` 창**으로 엽니다. 탭·주소창이 없어 데스크톱 앱처럼 보입니다.
    - **⚠️ tkinter로 만들지 마세요. 이미 해봤고 실패했습니다** (`src/gui_console.py`,
      지우지 않고 남겨 둠). 이 맥(macOS 26.5.2)의 파이썬은 CommandLineTools 3.9뿐이고
      **Tk가 8.5**입니다 — 2010년판 deprecated Aqua Tk라 **창은 뜨지만 Tk가 직접 그리는
      위젯이 하나도 렌더링되지 않습니다.** 네이티브 위젯(tk.Button)만 보이는 게 증상이고,
      위젯 배치·색은 정상이라(측정하면 전부 mapped) 원인을 찾기 매우 어렵습니다.
      되살리려면 Tk 8.6+ 파이썬(brew python@3.13 + python-tk)을 깔아야 합니다.
    - **`--user-data-dir` 를 빼지 마세요.** 없으면 크롬이 이미 떠 있을 때 기존 세션으로
      넘겨 그냥 **탭으로** 열리고 앱 창이 안 뜹니다. 전용 프로필은 `.chrome-console/`.
    - `arch -arm64` 도 필수입니다. Finder 실행은 x86_64로 떠서 arm64 numpy import가
      깨집니다. 판정은 `uname -m`(번역된 프로세스 기준이라 속음)이 아니라
      `sysctl -n hw.optional.arm64` 로 합니다.
    - 실행 로그는 **`journal/logs/console.log`**. 창이 안 뜬다는 얘기가 나오면 여기부터.
    - **현재가는 토스 API에서 받습니다** (2026-09-23). Yahoo의 SOXL/SOXS는 소급조정
      시계열이라 실제 호가와 2% 가까이 어긋납니다(실측: SOXL +1.97%, SOXS -1.95%).
      3배 ETF에서 그만큼 틀리면 평가손익이 통째로 어긋납니다. 심볼 3개를 **한 번의
      호출**로 받습니다(`/api/v1/prices?symbols=SOXX,SOXL,SOXS`).
    - **⚠️ `config/TRADING` 락을 없애지 마세요.** 토스 토큰은 client당 1개만 유효해서,
      콘솔이 토큰을 새로 받으면 매매 프로세스의 토큰이 무효가 됩니다. 그런데
      `toss_api._request()` 에는 **401 재시도가 없어** 그 순간 주문이 그냥 실패하고,
      SWITCH 중간이면 **매도만 되고 매수가 빠집니다.** 그래서 `daily_trade.sh` 가 도는
      동안 락을 만들고, 콘솔은 그 동안 Yahoo로 물러납니다(화면에 "시세 Yahoo(매매 중)"
      으로 표시). 락은 `trap ... EXIT` 로 어떤 경로로 끝나든 지워집니다.
      **조회 정확도보다 주문이 우선입니다.**
  - **웹 버전 (폰에서 볼 때만)**: `scripts/serve.sh` → `src/server.py`. `http.server`만 쓰고
    공용 venv에 웹 프레임워크를 넣지 않았습니다. 접속 토큰은 `config/server_token`에
    있고 맥과 폰이 같은 와이파이에 있어야 합니다.
  - GUI는 `server.build_state()`를 **import해서 재사용**합니다. 상태 계산을 GUI 쪽에
    복제하지 마세요 — 두 화면이 서로 다른 숫자를 보여주게 됩니다.
- **대시보드는 스냅샷입니다.** `results/dashboard.html` 은 cron(`refresh_dashboard.sh`)이
  1시간마다 다시 만들지만, **claude.ai에 발행된 페이지는 재발행해야 갱신됩니다** —
  CSP가 외부 요청을 막고, 로컬 프로세스가 발행 페이지에 데이터를 넣을 방법이 없습니다.
  "자동으로 최신"이라고 말하지 마세요. 갱신 요청이 오면 스크립트를 돌린 뒤
  `results/dashboard.html` 경로를 그대로 다시 publish 하십시오(같은 URL 유지).
- **대시보드 주문내역은 journal 기준**입니다. 토스 주문 목록 API가 `clientOrderId` 를
  돌려주지 않아 증권사 내역만으로는 봇 주문과 수동 주문을 구분할 수 없습니다. 그래서
  `executor.record` 가 `id=<orderId>` 를 note에 남기고 `journal.bot_orders()` 가 그걸 읽습니다.
  이 기록 방식을 깨면 대시보드가 봇 주문을 잃습니다.
- 텔레그램 토큰은 `config/.env`에만 둡니다. 절대 소스나 로그에 출력하지 마세요.
- 알림 발송 실패가 알리미 자체를 멈추게 해서는 안 됩니다(`notifier.send`는 조용히 실패).

## 파라미터를 바꿀 때

- **학습구간 최적화는 검증구간에서 나빠집니다.** 좌표하강 88회로 학습 6.60→9.59배를 만들었더니
  검증(AI 국면)은 31.06→24.59배로 떨어졌습니다.
- 무작위 3,000개 탐색에서 학습 점수와 검증 성과의 상관은 **+0.31**뿐입니다.
  전체 조합은 3,145,728개지만 더 찾아도 얻을 게 거의 없습니다.
- 바꾸면 반드시 **손대지 않은 구간**으로 검증하고, 전 구간 수치만 보고하지 마세요.
- 2026-09-22 횡보 탐색에서 같은 결론이 재현됐습니다. 학습 최고 조합을 고르면 학습
  1.55→7.72배(+399%)인데 검증은 37.74→18.54배(**-51%**)였습니다. 학습-검증 상관
  피어슨 +0.49 / 스피어만 +0.43. 진입 z 1.65~1.90 고원은 신뢰할 만하지만
  개별 칸의 배수는 표본 35거래로 구분 불가입니다. STRATEGY.md 6.4 참고.

## 다음에 이어서 할 일

**레짐은 A(50일선+기울기20+가격20) 그대로 둡니다** — 2026-09-23에 직관에 맞추는 시도를
전부 측정했고 실패했습니다(STRATEGY.md 6.6). **일치율과 성과가 정확히 반대로 갑니다**:
사용자 정답지 일치 60%→74%로 올리면 최근 5년이 29.04배→6.95배, MDD -60.9%→-86.5%.
레짐 전환이 이 전략의 주요 청산 사유라 라벨이 자주 바뀌면 포지션이 계속 잘립니다.

- **다시 시도하지 마세요**: 20선 위치 기반(F/G), 50선+가격20(B), 20선 기울기(C),
  가격조건 데드밴드(±2~14% 전부 — 5년 창은 그럴듯해 보여도 전체 16년이 103→7~29배),
  큰 움직임 강제 분류. 측정표가 6.6에 있습니다.
- **대신 표시 전용 라벨을 뒀습니다**: `regime.describe()` — 20일 가격 변화 ±6% 밴드,
  정답지와 78% 일치. 알리미 헤더·대시보드에 "장세 참고"로 나옵니다.
  **`decide_action` 에는 절대 넣지 마세요.** 전환이 연 38회입니다.
- **장기 추세 거부권은 이미 적용 중입니다** (채택→제거→재채택, 2026-09-23 하루 동안
  전부. "수치 기준"·"아키텍처" 절과 STRATEGY.md 4.6 참고). 더 손댈 일은 아니고, 끄자는
  얘기가 다시 나올 때를 위해 이력만 남겨 둡니다.

## 기각된 방향 (다시 시도하지 마세요)

- **하락장 SOXS**: 추세추종 0.17배(승률 16%) / 숏 회귀 59배 / 현금 57배 vs **SOXL 롱 회귀 244배**
- **상승장 눌림목 대기**: 16.3년에 7번만 진입. 전체 21.86배 vs 추세추종 207.67배
- **레짐 완충장치**: 확인 지연(3일 30배 → 5일 9배), 히스테리시스, 청산 분리 모두 파라미터 민감도 극심
- **애프터장 체결**: 거래량이 정규장의 **0.0%**. 표시 가격에 체결 불가
- **프리장 체결**: 유동성은 있으나(49.4%) 개장 대비 이점 없음
- **레짐 MA 기간 축소** (15~30일). 8~9월 오분류는 잡히지만 전환이 341→671~761회로 두 배가 되어
  휩쏘 비용이 이득을 삼킵니다(전체 0.27~18.94배). 대신 가격 조건을 더하는 쪽이 통했습니다(STRATEGY.md 3.1).
- **오버나이트 갭 없애기** (2026-09-24, STRATEGY.md 7.2). "종가에 체결하거나 신호를
  시가 기준으로 하면 갭이 사라지지 않나"를 측정 — **시가 신호는 승률이 58.3%→47.0%로
  무너지고**(시가 시계열이 더 들쭉날쭉), 종가 체결은 룩어헤드인 데다 **토스가 정규장
  종료 1시간 전까지만 주문을 받아 MOC 자체가 불가**합니다. 갭이 없는 이상적 케이스(C)도
  23.77배로 현재(41.75배)보다 낮지만, **A와 C의 차이는 34대26으로 잡음**이니
  "갭이 이득"이라고 말하지 마세요. 실험 스크립트 `scripts/backtest_timing.py`.
- **다른 종목으로 확장** (2026-09-24, STRATEGY.md 6.7). TQQQ/SQQQ·SPXL/SPXS·TNA/TZA·
  FAS/FAZ·LABU/LABD·TSLL/TSLQ·NVDL/NVD 7쌍에 **같은 규칙 그대로** 적용 — 전부 실패
  (승률 35~51%, 상위 2건 빼면 0.07~0.69배). 실험 스크립트 `scripts/backtest_other_pairs.py`.
  확장하려면 **규칙을 그대로 둔 채** 통과하는 자산을 찾아야 합니다.
- **레짐 전환 시 같은 방향 숏 유지** (2026-09-24, STRATEGY.md 4.8). "횡보 숏을 들고
  하락으로 전환되면 그대로 보유"를 두 해석으로 측정 — 추세 숏 전환은 전 구간 악화
  (검증 25.52→21.51배, 전체 159→111배. 16.3년 6건 중 5건이 더 나빴고 그 6건의 누적비
  1.43배가 전체 차이를 정확히 설명), 횡보 규칙 유지는 검증 25.52→25.41·전체 159→155로
  채택 기준(양쪽 개선) 미달인 3승 3패. 이유는 구조적입니다 — **하락 레짐의 종료 조건이
  곧 반등**이라 레짐 이탈까지 숏을 들면 반등을 반드시 맞습니다(같은 메커니즘이 79건
  표본에서 이미 죽었습니다: 하락장 SOXS 추세추종 0.17배). 실험 스크립트
  `scripts/backtest_regime_carry.py`.
  - **반대로 롱 쪽은 이미 그렇게 동작합니다** — 횡보 롱 보유 중 상승 전환은 목표가
    그대로 long이라 `RESTAMP`(매매 없이 규칙 갱신)이 됩니다. 끄면 나빠집니다
    (전체 159→152배). 같은 제안이 다시 오면 이 구분부터 짚으세요.
- **분할매수** (2026-09-23, STRATEGY.md 4.7). z 피라미딩·시간분할·물타기 3방식 × 횡보만·
  횡보+하락·전체 3범위 = 9조합을 최근 5년 학습/검증으로 측정 — **전부 검증 구간이
  기존(26.40배)보다 나쁨**(14.86~20.98배, 최악은 all/adverse_move 5.93배). 이 전략은
  이기는 거래가 며칠 안에 빨리 끝나고 지는 거래만 오래 물리므로, tranche를 늦게 채우는
  방식은 이기는 거래엔 자본을 덜 태우고 지는 거래엔 더 태운다. 실험 스크립트는
  `scripts/backtest_split_buy.py`. 다시 시도하려면 이 구조적 특성부터 바꿔야 한다.

## 관련 프로젝트

`../soxl-soxs-bot`은 200일선 레짐 버전입니다. 전체 기간 367배(MDD -54%)로 그쪽이 낫고,
AI 국면은 이쪽이 낫습니다(31배 vs 18배). 두 프로젝트를 섞지 마세요.
