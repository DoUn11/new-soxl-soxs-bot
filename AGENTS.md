# AGENTS.md

## 프로젝트 개요

SOXX 레짐 분류 기반 SOXL/SOXS 전략입니다. 설계와 근거는 [README.md](README.md)와
[docs/STRATEGY.md](docs/STRATEGY.md)에 있으며, 거래 로직을 변경하기 전에 반드시 먼저 읽어야 합니다.

## 기본 원칙과 제약

- 속도보다 안전성을 우선합니다.
- **자동 주문 실행 코드가 존재합니다** (`executor.py`, `toss_api.py`, `alerter.py --live`).
  2026-09-22 사용자 지시로 추가했습니다. 원래 원칙은 "실거래 검증(3단계)이 끝나기 전까지
  만들지 않는다"였고 **그 단계를 건너뛴 상태**입니다. 기록을 지우지 말고 이 사실을 유지하세요.
- **기본값은 항상 주문을 내지 않는 쪽입니다.** 실제 주문은 `--live` 를 명시해야만 나갑니다.
  이 기본값을 바꾸거나 안전장치를 우회하는 코드를 추가하지 않습니다.
- 사용자의 명시적 동의 없이 자동 주문 실행 기능을 활성화하지 않습니다.
- 안전장치(킬 스위치 `config/HALT`, **개장 후 120분 창**, 잔고 대조, 금액·건수 상한, 멱등키)를
  약화시키는 변경은 사용자에게 먼저 확인합니다.
- **체결 시각 창을 넓히지 마십시오.** 1시간봉 35건 실측에서 개장이 최적이고 12:30부터
  반토막입니다(STRATEGY.md 7.1). 개장을 놓치면 그날을 건너뛰는 것이 설계 의도입니다.
- 신호 기준 종목은 SOXX이며, 실제 거래 대상은 SOXL/SOXS입니다.
- API 키, 토큰, 실제 인증 정보를 저장소에 커밋하지 않습니다. `config/.env`는 gitignore 대상입니다.
- **성과를 보고할 때는 반드시 매수보유 벤치마크(SOXL, SOXX)를 나란히 제시합니다.**
  강세장에서는 어떤 전략이든 좋아 보이므로, 벤치마크 없는 수익률은 아무것도 말해주지 않습니다.

## 반드시 읽어야 할 문서

1. [README.md](README.md)
2. [docs/STRATEGY.md](docs/STRATEGY.md)
3. 환경변수가 관련된 경우 [config/.env.example](config/.env.example)

## 아키텍처

```
src/
├── data_fetcher.py   # 시세 수집 (yfinance). 토스 API 미연동
├── indicators.py     # 볼린저밴드, ADX
├── regime.py         # 레짐 분류 (50일선 + 20일 기울기 + 가격 20일 변화)
├── strategy.py       # 레짐별 규칙 + decide_action (백테스트·실거래 공용 판단)
├── risk_manager.py   # 포지션 사이징
├── backtest.py       # 과거 시뮬레이션
├── alerter.py        # 상태 조립 + 실행 + 보고 (판단은 strategy에 위임)
├── executor.py       # 사이징·안전장치·주문 전송 (전략 판단 없음)
├── toss_api.py       # 토스증권 Open API 클라이언트 (HTTP만, 전략·안전장치 없음)
├── notifier.py       # 텔레그램 발송
│   (대시보드: scripts/build_dashboard.py + dashboard/template.html — 조회 전용)
└── journal.py        # 거래 기록
```

경계를 지키세요: `toss_api.py`에 전략이나 안전장치를 넣지 않고, `executor.py`에 전략 판단을
넣지 않습니다. 안전장치는 `executor.preflight()` 한 곳에 모읍니다.

각 책임을 이 경계에 맞게 유지합니다. 데이터 수집, 레짐 판정, 신호 생성, 리스크 로직,
알림, 기록이 한 파일에 섞이지 않도록 합니다.

## 코딩 규칙

- 큰 리팩터링보다 작은, 검증 가능한 단위 변경을 선호합니다.
- 신호 로직은 읽기 쉽고 설명 가능해야 합니다.
- 거래 규칙을 변경할 때는 이유와 예상 영향을 문서나 주석에 남깁니다.
- 암묵적 매직 넘버보다 명시적이고 결정적인 로직을 선호합니다.
- 새 스크립트나 데모에서 기본적으로 실거래 기능을 구현해선 안 됩니다.

## 검증 가이드

수정이 완료되었다고 주장하기 전에:

- `./.venv/bin/python tests/test_executor.py` — 안전장치·멱등성·부분봉·체결시각 63건. 주문 관련 코드를
  건드렸다면 반드시 돌립니다. 증권사 호출은 스텁이므로 **실제 API 연동은 검증되지 않습니다.**
- `./.venv/bin/python tests/test_consistency.py` — 백테스트와 알리미가 같은 판단을 내리는지
  16.3년 4,088거래일 대조. **판단 로직이나 포지션 상태 조립을 건드렸다면 반드시 돌립니다.**
  판단은 `strategy.decide_action()` 한 곳에만 둡니다. 어느 쪽에든 복제하면 이 테스트가 깨집니다.
- `./.venv/bin/python -m compileall src` 로 문법을 점검합니다.
- 백테스트를 실제로 돌려 수치가 문서와 일치하는지 확인합니다.
- 확인하지 않았다면 성공을 가정하지 않고 명시적으로 말합니다.
- **실API 검증은 일부만 됐습니다** (2026-09-22): 조회 전부와 수량 기반 LIMIT 주문·체결
  확인은 통과, **`orderAmount`(금액 주문)와 SWITCH 연속 자금은 미검증**입니다.
  `toss_api.py` 독스트링의 항목별 현황을 보고 "검증됨"의 범위를 정확히 말하세요.

## 파라미터를 바꿀 때

- **학습구간 최적화는 검증구간에서 나빠집니다.** 좌표하강 88회로 학습 6.60→9.59배를 만들었더니
  검증(AI 국면)은 31.06→24.59배로 떨어졌습니다. 조정 전 이 사실을 먼저 떠올리십시오.
- 파라미터를 바꾸면 반드시 **손대지 않은 구간**으로 검증합니다. 전 구간 성과만 보고하면 과대평가입니다.
- 무작위 3,000개 탐색에서 학습 점수와 검증 성과의 상관은 +0.31뿐이었습니다.
  성과는 파라미터가 아니라 **구조**에서 나옵니다.

## 이미 검증해서 기각된 방향

다시 시도하지 마십시오. 근거는 docs/STRATEGY.md에 있습니다.

- **하락장 SOXS 보유** (추세추종 0.17배 / 숏 회귀 59배 / 현금 57배 vs SOXL 롱 회귀 244배)
- **상승장 눌림목 대기** (16.3년에 7번만 진입, 전체 21.86배 vs 추세추종 207.67배)
- **파라미터 좌표하강·무작위 탐색** (검증구간 악화)
- **레짐 완충장치** (확인 지연·히스테리시스·청산 분리 — 파라미터 민감도 극심)
- **애프터장 체결** (거래량이 정규장의 0.0%라 표시 가격에 체결 불가)
- **레짐 MA 기간 축소** (15~30일). 8~9월 오분류는 잡히지만 전환이 341→671~761회로 두 배가 되어
  휩쏘 비용이 이득을 삼킵니다(전체 0.27~18.94배). 대신 가격 조건을 더하는 쪽이 통했습니다(STRATEGY.md 3.1).

## AI 에이전트용 안전 체크리스트

주문 실행이나 리스크와 관련된 수정 전에는 아래를 확인합니다:

- docs/STRATEGY.md에 정의된 전략과 일치하는지
- **`--live` 없이도 주문이 나갈 수 있게 바뀌지 않았는지** (절대 금지)
- `executor.preflight()` 의 안전장치가 그대로인지, 우회 경로가 생기지 않았는지
- 소스 파일·로그·예외 메시지에 API 자격증명이 새지 않는지
- 백테스트와 실행 경로의 동작이 어긋난 채로 자동화가 확대되지 않는지
- `tests/test_executor.py` 를 돌렸는지
