# ============================================================================
# 비밀 — 절대 커밋하지 말 것
# ============================================================================
.env
config/.env

# 레짐 콘솔 웹 접속 토큰
config/server_token

# ============================================================================
# 로컬 운영 상태 (머신마다 다르고, 소스가 아님)
# ============================================================================

# 킬 스위치 / 매매 중 락
config/HALT
config/TRADING

# 대시보드 스냅샷, 증권사 주문 조회 결과
results/

# 실거래 기록 — 개인 금융기록(주문 금액·수량·증권사 주문ID)이고
# 봇이 돌 때마다 바뀌는 런타임 상태다. journal/README.md 만 올린다.
journal/trades.csv
journal/logs/

# 레짐 콘솔 전용 크롬 프로필 (쿠키·세션 포함)
.chrome-console/

# ============================================================================
# 백업 / 파이썬 / macOS
# ============================================================================
*.bak
*.bak-*
__pycache__/
*.pyc
.DS_Store
.venv
